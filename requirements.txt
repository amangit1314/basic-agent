# ARC Notice Extraction Agent — Dependencies
# ============================================

# Pydantic — Type-safe data models (the "vocabulary" of the agent)
pydantic>=2.0.0

# Browser Automation — Playwright drives a real Chromium browser
playwright>=1.40.0

# HTML Parsing — BeautifulSoup extracts links and text from HTML
beautifulsoup4>=4.12.0
lxml>=5.0.0

# HTTP Client — For downloading PDFs and simple requests
requests>=2.31.0
urllib3>=2.0.0

# PDF Extraction — Read text from PDF documents
pypdf>=4.0.0

# AI Provider — Google Gemini for reasoning and data extraction
google-generativeai>=0.8.0

# Environment Variables — Load .env files securely
python-dotenv>=1.0.0
